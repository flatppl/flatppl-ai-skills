[workspace]
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "osx-arm64", "win-64"]

[dependencies]
python = ">=3.10"

[tasks]
lint = "python scripts/lint_skills.py"
build = "python scripts/build_skills.py"
check = { depends-on = ["lint", "build"] }
